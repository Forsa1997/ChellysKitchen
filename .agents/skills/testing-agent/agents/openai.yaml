interface:
  display_name: "Testing Agent"
  short_description: "TDD specialist (prefer routed via orchestrator)"
  default_prompt: "Use $testing-agent via $multi-agent-coordinator to define failing-first tests and enforce TDD gates."
