interface:
  display_name: "Feature Developer"
  short_description: "Implementation specialist (prefer routed via orchestrator)"
  default_prompt: "Use $feature-developer via $multi-agent-coordinator to implement this change with minimal, testable edits."
