interface:
  display_name: "PM Planner"
  short_description: "Optional legacy planning skill for broad, ambiguous initiatives"
  default_prompt: "Use $multi-agent-coordinator only when explicit orchestration or PM-style synthesis is needed."
