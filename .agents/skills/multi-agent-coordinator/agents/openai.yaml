interface:
  display_name: "Product Manager Orchestrator"
  short_description: "Single entry point that coordinates specialist agents"
  default_prompt: "Use $multi-agent-coordinator as the single entry point to plan, delegate, and synthesize this request across the team."
