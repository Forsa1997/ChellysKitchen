interface:
  display_name: "UI Designer"
  short_description: "Design specialist (prefer routed via orchestrator)"
  default_prompt: "Use $ui-designer via $multi-agent-coordinator to translate this request into a clear, responsive interface."
