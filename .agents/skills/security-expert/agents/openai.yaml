interface:
  display_name: "Security Expert"
  short_description: "Security specialist (prefer routed via orchestrator)"
  default_prompt: "Use $security-expert via $multi-agent-coordinator to review this task for vulnerabilities and hardening steps."
